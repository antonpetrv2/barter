# Бързи връзки и Насоки за развитие на BARTER

## 🇧🇬 ЕЗИК

⚠️ **ПРАВИЛО**: Използвай **САМО БЪЛГАРСКИ** език
- ❌ Без руски
- ❌ Без македонски
- ✅ Само български

---

## 📌 Бързи връзки

### Входни точки
- **Начало**: [src/main.js](src/main.js)
- **Вход/Регистрация**: [src/pages/auth.js](src/pages/auth.js)
- **Админ панел**: [src/pages/admin.js](src/pages/admin.js)
- **Создаване обява**: [src/pages/createListing.js](src/pages/createListing.js)

### Услуги
- **Supabase**: [src/services/supabaseService.js](src/services/supabaseService.js)
- **Качване файлове**: `storageService` в supabaseService.js
- **Администриране**: `adminService` в supabaseService.js

### Компоненти
- **Навбар**: [src/components/navbar.js](src/components/navbar.js)
- **Качване изображения**: [src/components/imageUpload.js](src/components/imageUpload.js)
- **Подножие**: [src/components/footer.js](src/components/footer.js)

### Конфигурация
- **Supabase настройки**: [.env.local](.env.local)
- **SQL схема**: [database.sql](database.sql)
- **Администраторски гайд**: [ADMIN_SETUP.md](ADMIN_SETUP.md)

---

## ✅ Стандарти за кодиране

### Именуване
- **Файлове**: camelCase (например: `imageUpload.js`)
- **CSS класове**: kebab-case (например: `.image-upload`)
- **JavaScript променливи**: camelCase (например: `uploadedImages`)
- **Функции**: camelCase + глагол (например: `handleCreateListing()`)

### Структура
- Всяка функция да има JSDoc коментар
- Функции да са малки (до 50 редове)
- Сепарация на логика и UI

### Стилизация
- Bootstrap 5 класове за layout
- CSS variables за консистентност
- Mobile-first подход

---

## 🔧 Команди

```bash
# Начало на development
npm run dev

# Production build
npm run build

# Preview production версия
npm run preview
```

---

## 🗄️ Структура на базата

### Основни таблици
| Таблица | Описание |
|---------|----------|
| `users` | Профили на потребители |
| `listings` | Обяви за продажба |
| `messages` | Съобщения между потребители |
| `reviews` | Отзиви и рейтинги |
| `categories` | Категории на обяви |

### Колони `users`
- `id` (UUID) - уникален идентификатор
- `email` - адрес за контакт
- `role` - 'user' или 'admin'
- `status` - 'pending', 'approved' или 'rejected'
- `is_banned` - дали е блокиран

---

## 👤 Администраторски функции

### Логване като админ
1. Регистрация на нов потребител
2. SQL команда:
```sql
UPDATE users 
SET role = 'admin', status = 'approved' 
WHERE email = 'твоя@имейл.бг';
```
3. Щракни иконата ↻ в навбара

### Админ панел функции
- ✅ Одобряване/отхвърляне потребители
- 🚫 Блокиране потребители
- 👑 Назначаване администратори
- 📊 Статистика на системата

---

## 📦 Качване на файлове

### Създай Storage Bucket
1. Supabase > Storage
2. "New bucket" > `listings`
3. Включи "Public bucket"

### Код за качване
```javascript
import { storageService } from '../services/supabaseService.js'

const { url, error } = await storageService.uploadImage(file)
```

---

## 🔐 Суперспособност за разработчика

### Бързо тестване
```javascript
// В браузъра (Console)
console.log(window.authState)  // Статус на логване
console.log(window.uploadedImages)  // Качени снимки
```

### Debug режим
```javascript
// Логване всичко
localStorage.setItem('debug', 'true')
```

---

## 📝 Следващи функции

- [ ] Редактиране/Изтриване обяви
- [ ] Съобщения между потребители
- [ ] Оценки и отзиви
- [ ] Разширено търсене
- [ ] Email нотификации

---

## ⚡ Производителност

### Оптимизация
- Lazy loading за снимки
- Кеширане на данни
- Минимален JavaScript

### Тестване
1. DevTools Network tab
2. Провери размер на бандъла
3. Тест на мобилни устройства

---

## 🚨 Важни моменти

⚠️ **ВСЕГДА проверявай**:
1. RLS политиките в Supabase са отключени за развитие
2. `.env.local` файлът НЕ е в git
3. Всички променливи са на български

✅ **ПРЕДИ commit**:
- Тест на админ панела
- Тест на качване на снимки
- Проверка на логване/регистрация

---

## 🐛 Частни грешки

### "Cannot coerce the result to a single JSON object"
**Решение**: Отключи RLS в Supabase таблиците

### "getUserProfile is not a function"
**Решение**: Проверка дали функцията е в `authService`

### Снимките не се качват
**Решение**: Проверка дали `listings` bucket съществува и е public

---

## 📞 Ресурси

- [Supabase документация](https://supabase.com/docs)
- [Bootstrap 5 компоненти](https://getbootstrap.com/docs/5.0)
- [Vite гайд](https://vitejs.dev)
- [JavaScript MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript)

---

**Последна актуализация**: 26 януари 2026

*Този файл трябва да се актуализира след всяка нова функция*
